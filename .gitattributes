# Auto-detect text files and normalize line endings to LF in the repo.
# On checkout, Git converts to the platform's native convention.
* text=auto

# Explicitly binary
*.gguf binary
*.png  binary
*.jpg  binary
*.ico  binary
